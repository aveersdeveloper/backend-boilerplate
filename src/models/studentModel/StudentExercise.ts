import mongoose from "mongoose";

// Exercise:
// 1. Extend the Model: Add new fields such as `college` and `place` to the Student model.
//    Ensure these fields are set as required, just like the existing `name` and `course` fields.
